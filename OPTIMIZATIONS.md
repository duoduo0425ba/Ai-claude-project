# 项目改进与优化建议 (Project Improvements & Optimization Suggestions)

本文档整理了对“零花钱记账 (Pocket Money Tracker)”项目的潜在改进方向，涵盖性能、体验、功能及安全性四个维度。

---

## 🚀 1. 性能优化 (Performance)

### 1.1 后端分页 (Server-side Pagination)
*   **现状**: 目前在前端进行分页（每页 30 条），随着交易记录增加，API 响应体积会线性增长。
*   **建议**: 在 SQL 查询中使用 `LIMIT` 和 `OFFSET`。
*   **收益**: 极大减少首屏加载流量，支持海量数据流畅展示。

### 1.2 数据库索引优化
*   **现状**: `transactions` 表目前只有主键索引。
*   **建议**: 为 `date`、`category` 和 `type` 字段创建复合索引。
    ```sql
    CREATE INDEX idx_transactions_date_type ON transactions(date, type);
    ```
*   **收益**: 显著提升统计图表（按月/年聚合）的查询速度。

---

## ✨ 2. 用户体验 (UX & DX)

### 2.1 数据缓存与同步 (TanStack Query)
*   **现状**: 页面切换时频繁显示 Loading，缺乏数据持久化缓存。
*   **建议**: 引入 `TanStack Query` (React Query) 替代简单的 `useEffect` 获取数据。
*   **收益**: 实现“秒开”体验，支持后台静默刷新和自动错误重试。

### 2.2 离线优先与 PWA
*   **现状**: 应用依赖后端 API 运行，断网时无法查看或记录。
*   **建议**: 集成 `vite-plugin-pwa`，并利用 Service Worker 缓存静态资源。
*   **收益**: 支持离线记账，应用可像原生 App 一样安装到桌面/主屏幕。

### 2.3 微交互动画 (Framer Motion)
*   **现状**: 页面切换、弹窗出现较为生硬。
*   **建议**: 使用 `Framer Motion` 为路由切换和列表增删添加平滑过渡。
*   **收益**: 提升产品的精致感和高级感。

---

## 🛠️ 3. 功能增强 (Features)

### 3.1 周期性账单 (Recurring Transactions)
*   **现状**: 房租、订阅费等重复支出需手动录入。
*   **建议**: 增加“周期账单”模板，支持按周/月自动生成记录。
*   **收益**: 减少重复劳动，提高用户留存率。

### 3.2 智能分类预测
*   **现状**: 备注与分类完全解耦。
*   **建议**: 根据用户历史记录（备注关键词），在输入备注时自动推荐分类。
*   **收益**: 进一步加快记账速度。

### 3.3 多币种支持
*   **现状**: 仅支持单一货币。
*   **建议**: 允许设置本位币，并在记账时支持汇率转换。
*   **收益**: 扩展海外旅行或海淘场景。

---

## 🔒 4. 安全与可靠性 (Security & Reliability)

### 4.1 输入验证 (Schema Validation)
*   **现状**: 后端直接接收前端数据，缺乏严格的类型和范围检查。
*   **建议**: 在 Express 层引入 `Zod` 或 `Joi` 进行 Schema 验证。
*   **收益**: 防止异常输入（负数、非法日期、脚本注入）污染数据库。

### 4.2 自动化测试
*   **现状**: 缺乏自动化校验手段。
*   **建议**: 
    *   **后端**: 使用 `Supertest` + `Jest` 测试统计 API 的计算准确性。
    *   **前端**: 使用 `Vitest` 测试复杂的 Excel 导入导出工具函数。
*   **收益**: 保证核心财务逻辑在重构时不会出错。

### 4.3 数据备份与隐私
*   **现状**: 数据仅存储在本地 `data.db`。
*   **建议**: 
    *   提供“备份到 JSON/CSV”的导出功能。
    *   利用 macOS 原生 API (Biometrics) 为 `.app` 增加指纹解锁。
*   **收益**: 保护用户隐私，防止意外数据丢失。

---

## 📈 优先级推荐
1.  **后端分页** (基础稳定性)
2.  **TanStack Query** (流畅度提升)
3.  **周期性账单** (核心功能补完)
4.  **输入验证** (系统健壮性)
